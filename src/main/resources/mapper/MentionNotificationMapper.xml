<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ocean.repository.MentionNotificationRepository">
    <insert id="insertNew">
        INSERT INTO `MENTION_NOTIFICATION` (
            EVENT_CD, USER_ID, NOTI_STATE, READ_STATE
        ) VALUES (
            #{eventCd}, #{userId}, #{notiState}, #{readState}
        )
    </insert>

    <select id="selectUserNoti" parameterType="String" resultType="com.example.ocean.dto.request.MentionNotification">
        SELECT  EVENT_CD AS eventCd, USER_ID AS userId,
                NOTI_STATE AS notiState, READ_STATE AS readState
        FROM   `MENTION_NOTIFICATION`
        WHERE   USER_ID = #{userId}
    </select>

    <update id="updateAllNoti" parameterType="String">
        UPDATE `MENTION_NOTIFICATION`
        SET     READ_STATE = '1'
        WHERE   USER_ID = #{userId}
    </update>

    <update id="updateNoti" parameterType="com.example.ocean.dto.request.ReadNotiRequest">
        UPDATE `MENTION_NOTIFICATION`
        SET     READ_STATE = '1'
        WHERE   EVENT_CD = #{eventCd}
        AND     USER_ID = #{userId}
        AND     NOTI_STATE = #{notiState}
    </update>

</mapper>